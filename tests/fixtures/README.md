# Synthetic test fixture

`routerbench_sample.csv` is an original, deterministic fixture containing 48
rows: 12 synthetic prompts multiplied by four anonymous model arms. It exists
only to test schema and pipeline behavior.

It is **not** copied from RouterBench, is **not** a sample of user traffic, and
must never be cited as experimental evidence.
